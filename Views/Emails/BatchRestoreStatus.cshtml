@inject Microsoft.Extensions.Localization.IStringLocalizer<MailArchiver.SharedResource> Localizer
@model MailArchiver.Models.BatchRestoreJob
@{
    ViewData["Title"] = Localizer["BatchRestoreStatusTitle"];
    var shouldAutoRefresh = Model.Status == BatchRestoreJobStatus.Running || Model.Status == BatchRestoreJobStatus.Queued;
}

<div class="container">
        @await Html.PartialAsync("_BatchRestoreStatusLiveContent", Model)

</div>

<!-- Bootstrap Modal for Cancel Confirmation -->
<div class="modal fade" id="cancelJobModal" tabindex="-1" aria-labelledby="cancelJobModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="cancelJobModalLabel">@Localizer["CancelJob"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @Localizer["CancelJobConfirm"]
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@Localizer["Close"]</button>
                <form asp-action="CancelBatchRestore" method="post" style="display: inline;">
                    <input type="hidden" name="jobId" value="@Model.JobId" />
                    <input type="hidden" name="returnUrl" value="@Model.ReturnUrl" />
                    <button type="submit" class="btn btn-warning">
                        @Localizer["CancelJob"]
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
